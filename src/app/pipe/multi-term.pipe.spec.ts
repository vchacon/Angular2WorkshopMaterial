import { MultiTermPipe } from './multi-term.pipe';

describe('MultiTermPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiTermPipe();
    expect(pipe).toBeTruthy();
  });
});
